
p53:
  methods:
    exact:
      epochs: 150
    variational:
      epochs: 300
    mcmc:
      epochs: 2000

hafner:
  num_latents: 1
  methods:
    variational:
      epochs: 300
    mcmc:
      epochs: 2000

toy:
  num_latents: 3
  methods:
    variational:
      epochs: 300
    mcmc:
      epochs: 2000

toy_spatial:
  num_latents: 1
  methods:
    partial:
      epochs: 100

drosophila:
  num_latents: 1
  methods:
    partial:
      epochs: 60

mnist:
  methods:
    variational:
      epochs: 300
