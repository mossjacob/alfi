# p53 networks
p53:
  methods:
    exact:
      epochs: 150
      report_interval: 5
    variational:
      epochs: 300
      report_interval: 10
    mcmc:
      epochs: 2000

hafner:
  num_latents: 1
  methods:
    variational:
      epochs: 300
      report_interval: 10
    mcmc:
      epochs: 2000

# Toy datasets
toy:
  num_latents: 3
  methods:
    variational:
      epochs: 300
    mcmc:
      epochs: 2000

toy_spatial:
  num_latents: 1
  methods:
    partial:
      epochs: 100
      report_interval: 1

# Drosophila Experiments
dros_kr:
  num_latents: 1
  methods:
    partial:
      epochs: 60
      report_interval: 1
dros_kni:
  num_latents: 1
  methods:
    partial:
      epochs: 60
      report_interval: 1
dros_gt:
  num_latents: 1
  methods:
    partial:
      epochs: 60
      report_interval: 1

# Other stuff
mnist:
  methods:
    variational:
      epochs: 300
