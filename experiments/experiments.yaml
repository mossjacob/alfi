
p53:
  methods:
    exact:
      epochs: 150
      report_interval: 5
    variational:
      epochs: 300
      report_interval: 10
    mcmc:
      epochs: 2000

hafner:
  num_latents: 1
  methods:
    variational:
      epochs: 300
      report_interval: 10
    mcmc:
      epochs: 2000

toy:
  num_latents: 3
  methods:
    variational:
      epochs: 300
    mcmc:
      epochs: 2000

toy_spatial:
  num_latents: 1
  methods:
    partial:
      epochs: 100
      report_interval: 1

drosophila:
  num_latents: 1
  methods:
    partial:
      epochs: 60
      report_interval: 1
mnist:
  methods:
    variational:
      epochs: 300
